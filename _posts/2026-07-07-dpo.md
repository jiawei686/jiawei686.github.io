---
layout: post
title: "DPO: Direct Preference Optimization"
date: 2026-07-07
tags: [llm]
subcat: alignment
---

**Paper:** Rafailov, R., et al., *Direct Preference Optimization: Your Language Model is Secretly a Reward Model*, NeurIPS 2023. [arXiv:2305.18290](https://arxiv.org/abs/2305.18290)

RLHF works, but it's a chore: train a reward model, then run fragile, sample-hungry RL (PPO) on top. DPO collapses that two-stage pipeline into **one supervised-style loss**. The trick is that the optimal policy of KL-constrained RLHF has a closed form, and once you plug it into the Bradley–Terry model the whole problem becomes a classification objective. No reward model, no RL sampling, no value network. It's the default open-weight alignment method now, and if you fine-tune a model to behave you probably reach for DPO first.

## The move: the LM *is* the reward model

Start from the standard RLHF objective (maximize reward, stay near a reference $\pi_{\text{ref}}$ via KL). Its optimal policy has the closed form:

$$
\pi^*(y \mid x) = \frac{1}{Z(x)}\,\pi_{\text{ref}}(y \mid x)\,\exp\!\Big(\tfrac{1}{\beta} r(x, y)\Big)
$$

Solve this for the reward: $r(x,y) = \beta \log \frac{\pi^*(y\mid x)}{\pi_{\text{ref}}(y\mid x)} + \beta \log Z(x)$. Substitute into Bradley–Terry and the reward model disappears, leaving a loss over preference pairs $(y_w \succ y_l)$:

$$
\mathcal{L}_{\text{DPO}} = -\,\mathbb{E}\left[\log \sigma\!\left(
\beta \log \frac{\pi_\theta(y_w\mid x)}{\pi_{\text{ref}}(y_w\mid x)}
- \beta \log \frac{\pi_\theta(y_l\mid x)}{\pi_{\text{ref}}(y_l\mid x)}
\right)\right]
$$

Only $\pi_\theta$ and a **frozen** $\pi_{\text{ref}}$ appear. Updating $\pi_\theta$ to push the chosen log-ratio above the rejected one is the whole training step, nothing else runs.

## Why people actually use it

- **Stable:** one classification loss, no RL rollouts, no critic.
- **Cheap:** two forward passes per sample (policy + reference), like SFT.
- **Effective:** matches or beats PPO-based RLHF on sentiment, summarization, and dialogue.

## Results, briefly

- On the TL;DR summarization benchmark, DPO controlled output sentiment better than PPO-RLHF and matched it on quality.
- Demonstrated that a simple, well-justified reparameterization removes the need for explicit reward modeling in most setups.

## Where it sits

DPO is the bridge between "I have preference data" and "I have an aligned model." It's the practical answer to the two RLHF posts, and you need it before the direct-alignment variants make sense (IPO, KTO, ORPO). My take: it trades the explicit reward model's debuggability for convenience. When a DPO model misbehaves you can't inspect the reward, you can only stare at log-ratios and guess.

## References

- Rafailov et al. (2023). *Direct Preference Optimization: Your Language Model is Secretly a Reward Model.* NeurIPS 2023. [arXiv:2305.18290](https://arxiv.org/abs/2305.18290)
- Christiano et al. (2017). *Deep Reinforcement Learning from Human Preferences.* [arXiv:1706.03741](https://arxiv.org/abs/1706.03741)
- Ouyang et al. (2022). *InstructGPT.* [arXiv:2203.02155](https://arxiv.org/abs/2203.02155)
